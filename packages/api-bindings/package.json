{
  "name": "@tine/api-bindings",
  "version": "0.2.0",
  "license": "MIT OR Apache-2.0",
  "repository": "https://github.com/tinecli/tine",
  "type": "module",
  "exports": {
    ".": {
      "tine-jsc": "./tine-jsc-stub.mjs",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "tine-jsc-stub.mjs"
  ],
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "clean": "rm -rf dist/",
    "build": "bun run clean && bun run generate-requests && tsc",
    "lint": "biome check src",
    "lint:fix": "biome check --write src",
    "prepublishOnly": "bun run build",
    "generate-requests": "bun codegen/generate-requests.ts && biome check --write src/requests.ts"
  },
  "dependencies": {
    "@tine/proto": "workspace:^",
    "@bufbuild/protobuf": "2.4.0"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.7",
    "@tine/tsconfig": "workspace:^",
    "@tsconfig/recommended": "^1.0.8",
    "@types/node": "^22.15.20",
    "@typescript/analyze-trace": "^0.10.1",
    "ts-morph": "^26.0.0",
    "typescript": "^7.0.2"
  }
}
